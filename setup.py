from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project is a binary classificaton problem to predict if a customer would be happy or not based on feedbacks on survey after service.',
    author='Dorcas Taiwo',
    license='MIT',
)
